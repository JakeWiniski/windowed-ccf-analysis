# windowed-ccf-analysis
This project provides a generalized Python workflow for performing windowed cross-correlation analysis (CCF) on time series data. It is designed to work out-of-the-box with any set of CSV files containing timestamped measurements, enabling fast batch processing and automated summary outputs.
